using Sirenix.OdinInspector;
using UnityEngine;

namespace StinkySteak.Rootdash.Manager
{
    public class CustomerProcessor : MonoBehaviour
    {
        [SerializeField][ReadOnly] private int _activeCustomerIndex = -1;

        private void Update()
        {
            
        }
    }
}